language: go
go:
 - stable

before_install:
 - mkdir -p $(go env GOPATH)/src/gopkg.in/virgil.v5
 - cp -a $(go env GOPATH)/src/github.com/VirgilSecurity/virgil-sdk-go/ $(go env GOPATH)/src/gopkg.in/virgil.v5/

script:

 - go test -v ./... -tags=integration